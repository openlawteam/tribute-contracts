---
id: nfts
title: nfts
---

No description

```graphql
nfts(
  skip: Int = 0
  first: Int = 100
  orderBy: NFT_orderBy
  orderDirection: OrderDirection
  where: NFT_filter
  block: Block_height
): [NFT!]!

```

### Arguments

#### `skip` ([`Int`](/schema/scalars/int))

#### `first` ([`Int`](/schema/scalars/int))

#### `orderBy` ([`NFT_orderBy`](/schema/enums/nft-order-by))

#### `orderDirection` ([`OrderDirection`](/schema/enums/order-direction))

#### `where` ([`NFT_filter`](/schema/inputs/nft-filter))

#### `block` ([`Block_height`](/schema/inputs/block-height))

The block at which the query should be executed. Can either be an `{ number: Int }` containing the block number or a `{ hash: Bytes }` value containing a block hash. Defaults to the latest block when omitted.

### Type

#### [`NFT`](/schema/objects/nft)
